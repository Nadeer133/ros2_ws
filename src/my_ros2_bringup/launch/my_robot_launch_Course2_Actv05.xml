<launch>
    <!-- 1. Define paths -->
    <let name="urdf_path" 
         value="$(find-pkg-share my_robot_description)/urdf/Course2_Actv01.xacro" />

     <!-- 1. Gazebo_Worlds paths -->    
    <let name="worlds_path" 
         value="$(find-pkg-share my_ros2_bringup)/worlds/test_world.sdf" />

    <!-- 1. bridging gazebo and ros2-->
    <let name="gazebo_config_path"
         value="$(find-pkg-share my_ros2_bringup)/config/gazebo_bridge.yaml" />

    <let name="rviz_config_path" 
         value="$(find-pkg-share my_robot_description)/rviz/Course2_Actv01_Urdf.rviz" />

    <!-- 2. Robot State Publisher -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description" value="$(command 'xacro $(var urdf_path)')" />
    </node>

    <!-- 3. launchin gazebo with emty simulation -->
    <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
        <arg name="gz_args" value="-r $(var worlds_path)"/>
        <!-- <arg name="gz_args" value="-r empty.sdf"/> -->
    </include> 

    <!-- 4. spawning the robot -->
    <node pkg="ros_gz_sim" exec="create" args="-topic robot_description"/>
        
    <!-- 5. Joint State Publisher GUI --> 
     <node pkg="joint_state_publisher_gui" exec="joint_state_publisher_gui" />
    
    <!-- 1. bridging gazebo and ros2-->
    <node pkg="ros_gz_bridge" exec="parameter_bridge">
          <param name="config_file" value="$(var gazebo_config_path)"/>
    </node> 

    <!-- 6. RViz2 node loading the custom config -->
    <node pkg="rviz2" exec="rviz2" output="screen" args="-d $(var rviz_config_path)" />
</launch>